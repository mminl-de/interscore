name: can we compile?

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest
    container: archlinux:latest

    steps:
    - uses: actions/checkout@v4
    - name: install deps
      run: sudo pacman -Syu json-c qt6-base qt6-multimedia
    - name: frontend
      run: make js
    - name: backend
      run: make b-install
    - name: rentnerend
      run: make r-install
