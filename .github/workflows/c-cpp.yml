name: can we compile?

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest
    container: archlinux:latest

    steps:
    - uses: actions/checkout@v4
    - name: install deps
      run: pacman -Syu --noconfirm make json-c qt6-base qt6-multimedia typescript gcc
    - name: frontend
      run: make js
    - name: backend
      run: make b-install
    - name: rentnerend
      run: make r-install
